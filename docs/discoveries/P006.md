# Discovery Report — P006: append subcmd (atomic write)

**Phiếu:** P006-append-atomic
**Ngày ship:** 2026-05-28
**Tầng:** 1 | **Lane:** Guarded (filesystem write)
**Worker:** Thợ (claude-sonnet-4-6)

---

## Tóm tắt

P006 wires `cli/append.rs` (stub → real), creates `src/inbox.rs` (new module), adds `impl Display for AdvisoryRow/Status/Severity` to `src/row.rs`, và updates `src/main.rs` dispatch arm với InboxError-aware exit code map. Đây là phiếu đầu tiên dùng INV-LOCAL-002 atomic-write protocol trên inbox file. Tất cả 41 tests pass, zero clippy warnings, zero fmt diff.

---

## Anchors — kết quả verify thực tế

| Anchor | Assumption | Kết quả |
|--------|-----------|---------|
| #1 | `cli/append.rs` là stub P001 | ✅ Stub xác nhận, signature khớp phiếu |
| #2 | `Commands::Append` + dispatch arm | ✅ Variant tại lines 40-47, dispatch tại line 124 |
| #3 | `--inbox` + `--rows-json` REQUIRED | ✅ Xác nhận qua `cargo run -- append --help` |
| #6 | Chưa có `impl Display` trên `AdvisoryRow` | ✅ 0 hits — đúng |
| #7/#8 | Deps `tempfile`/`thiserror`/`serde_json`/`chrono`/`anyhow`/`serde` + dev-deps `assert_cmd`/`predicates` | ✅ All confirmed in Cargo.toml |
| #9 | `as_file().sync_all()` chain | ✅ `pub fn as_file(&self) -> &F` tồn tại trong tempfile-3.27.0/src/file/mod.rs. Chain valid. |
| #10 | `NaiveDate::Display` = `YYYY-MM-DD` | ✅ Verified từ chrono source — `Display` delegates to `Debug`, uses `write_hundreds` = `YYYY-MM-DD` |
| #14 | README `append` quick-start | ✅ (partial) Chưa có section riêng — thêm trong Task 6.5.4 |
| #19 | `pub mod append;` trong `cli/mod.rs` | ✅ Line 7 |
| #20 | Chưa có `mod inbox;` trong `main.rs` | ✅ 0 hits — thêm trong Task 4 |

---

## Sai lệch giữa phiếu và thực tế

### SD-1 [mechanical]: Test baseline count wording trong phiếu (không ảnh hưởng behavior)

Phiếu ghi "baseline = 31 tests" — ĐÚNG. Tuy nhiên `cargo test -- --list | wc -l` trả về 37 vì nó đếm cả blank lines và summary rows `"N tests, 0 benchmarks"`. Thợ tính đúng = 31 (24 unit + 4 dedup integration + 3 parse_report integration). Không cần sửa phiếu.

### SD-2 [mechanical]: `insert_rows` signature — Architect đề xuất thêm `path: &Path` param

Phiếu §Giải pháp ghi `PathBuf::new()` placeholder cho `InboxError::MissingRowsHeading` và note "Architect recommendation: add `path: &Path` parameter to `insert_rows`". Thợ chọn theo recommendation: thêm `path: &Path` làm param thứ 3 của `insert_rows(content, rows, path)`. Quyết định Tầng 2 (self-decide). Error message giờ hiện đúng path trong terminal thay vì empty string.

**Impact:** Caller `cli/append.rs` truyền `&inbox_path` — phù hợp. Unit tests trong `inbox.rs` truyền `Path::new("inbox.md")` — acceptable placeholder.

### SD-3 [mechanical]: Doc drift ARCHITECTURE §7 `flush` vs INVARIANTS INV-LOCAL-002 `sync_all`

Anchor #17 đã flag drift: ARCHITECTURE §7 mô tả `temp.flush()?` nhưng INV-LOCAL-002 yêu cầu `temp.as_file().sync_all()?` (stricter). Thợ:
1. Implement `inbox.rs::write_atomic` theo INV (sync_all) — ĐÚNG.
2. Cập nhật ARCHITECTURE §7 để align với INV — doc drift resolved trong P006.

---

## Sub-mechanism verification

| Sub-mech | Check | Kết quả |
|----------|-------|---------|
| A (trigger) | N/A — không có hook/cron trong P006 | N/A |
| B (capability) | `cargo check` | ✅ exit 0, 0 warnings |
| B (capability) | `cargo test inbox` | ✅ 5 unit tests pass (insert happy / missing heading / empty rows / write_atomic round-trip / count_open_skips_html) |
| B (capability) | `cargo test row::tests::advisory_row_display_pipe_delim` | ✅ pass |
| B (capability) | `cargo test --test append_cli` | ✅ 4 integration tests pass |
| B (capability) | `cargo build --release` | ✅ exit 0, 0 warnings |
| C (migration) | N/A — không có schema change | N/A |
| D (persistence) | `grep -l "append" docs/ARCHITECTURE.md` | ✅ hit (§1 + §5) |
| D (persistence) | `grep -l "inbox.rs\|InboxError" docs/ARCHITECTURE.md` | ✅ hit (§5 scaffold-status) |
| D (persistence) | `grep -n "P006\|inbox.rs" docs/security/INVARIANTS.md` | ✅ "First concrete user" note added |
| D (persistence) | `grep -n "append" README.md` | ✅ quick-start section added |
| E (env drift) | `cargo update --dry-run` | ✅ 0 packages updated, no surprise bump |
| E (env drift) | `cargo build --release` from current target | ✅ exit 0 |
| F (runtime state) | `grep -E 'ghp_|...' src/inbox.rs src/cli/append.rs src/row.rs` | ✅ 0 hits — clean |
| F (runtime state) | INV-LOCAL-002 hand-check: `grep -n "NamedTempFile::new_in\|sync_all\|persist" src/inbox.rs` | ✅ 3 hits in `write_atomic` |
| F (runtime state) | Forbidden pattern check: `grep -E "OpenOptions::append\|std::fs::write.*inbox\|std::fs::rename" src/inbox.rs src/cli/append.rs` | ✅ Only doc comment hits, no code use |

---

## Files thay đổi

| File | Thay đổi |
|------|---------|
| `src/inbox.rs` | NEW: `InboxError`, `read_inbox`, `insert_rows` (path param added), `write_atomic`, `count_open_rows` (private), 5 unit tests |
| `src/row.rs` | ADD: `use std::fmt`, `impl Display for Status/Severity/AdvisoryRow`, 1 unit test |
| `src/cli/append.rs` | REWRITE: stub → real impl (RowsEnvelope inline, calls inbox module) |
| `src/main.rs` | ADD: `mod inbox;`; UPDATE: `Commands::Append` dispatch arm → InboxError downcast exit code map |
| `tests/fixtures/inbox-baseline.md` | NEW: 2 rows (1 open + 1 processed) + HTML comment placeholder |
| `tests/fixtures/rows-2.json` | NEW: 2 open rows envelope |
| `tests/append_cli.rs` | NEW: 4 integration tests |
| `docs/CHANGELOG.md` | ADD: P006 entry (top) |
| `docs/ARCHITECTURE.md` | UPDATE: §5 scaffold-status P006 shipped; §7 sync_all alignment (doc drift fix) |
| `docs/security/INVARIANTS.md` | UPDATE: INV-LOCAL-002 "First concrete user" note |
| `README.md` | ADD: `append` quick-start section |

---

## Follow-up items (không thuộc scope P006)

1. **ARCHITECTURE §7 dùng `mut temp` nhưng giờ `as_file()` không cần mut** — `cargo check` clean, no issue. Phiếu sau có thể cleanup comment nếu muốn.
2. **`RowsEnvelope` duplicate** — defined inline in both `cli/dedup.rs` và `cli/append.rs`. Phiếu sau (P009 scan-and-append) có thể move lên shared location nếu cần. Scope discipline: NOT P006.
3. **`total_open` heuristic** — `count_open_rows` dùng substring match `| open |`. Đủ cho MVP; có thể tighten via `split('|').nth(7)` nếu false-positive xuất hiện khi inbox data chứa literal `| open |` trong non-status columns. BACKLOG nếu cần.
4. **INV-LOCAL-002 chưa cover state-write path** — P007 migrate-state, P008 state-backfill sẽ là user tiếp theo. Phiếu đó phải re-verify INV-LOCAL-002 reference shape.

---

## Kết quả cuối

- `cargo build --release` ✅ 0 warnings
- `cargo test --all` ✅ 41 tests pass (30 unit + 11 integration)
- `cargo clippy --all-targets -- -D warnings` ✅ clean
- `cargo fmt --check` ✅ no diff
- `docs-gate --all --verbose` ✅ 4/4 pass

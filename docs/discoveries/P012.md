# Discovery Report — P012: Release polish (README + ARCHITECTURE + cargo publish --dry-run)

**Date:** 2026-05-28
**Phiếu:** P012-polish-publish
**Tầng:** 2 | **Lane:** Normal
**Worker:** Thợ (autonomous mode)

---

## Tóm tắt

Phiếu polish-only: không có code change, không có test change. Tất cả 5 tasks hoàn thành, 69 tests vẫn pass, `cargo publish --dry-run` exit 0.

---

## README.md trim

- **Before:** 243 lines (Architect estimate 244 — diff 1 line do trailing newline counting; không phải sai lệch có nghĩa)
- **After:** 158 lines
- **Trim ratio:** 85 lines removed (~35% reduction)
- **Method:** Xóa per-subcmd exit-code tables (thay bằng 1 dòng tham chiếu ARCHITECTURE §1), xóa duplicate atomic-write callouts (thay bằng 1 dòng INV-LOCAL-002 ở đầu CLI section), thu gọn per-subcmd narrative xuống 1 example + 1-line output preview, rút gọn Tools table (từ 3 cột xuống 2 cột, full schemas → ARCHITECTURE §6 link).
- **Preserved (P013 load-bearing):** `cargo install advisory-inbox`, `.mcp.json` example, `tools/call` JSON-RPC example, `<!-- INBOX_APPEND_START -->` occurrence (trong tools/call payload), link đến ARCHITECTURE.md.

---

## ARCHITECTURE.md §5 consolidation

- **Before:** Lines 245-257 (13 lines) — P001-P011 multi-line ship entries với technical detail đầy đủ từng phiếu.
- **After:** 2 lines — "Scaffold status (Phase 1-3 complete)" + "Pending phiếu" pointer.
- **Flow diagram:** ASCII format picked (universal — renders identically trên GitHub web + `less` terminal + plaintext viewers). Mermaid rejected vì phiếu này target cả terminal users. Diagram 5-box: sentinel → row → dedup → append → state, với lateral state/inbox file boxes.
- **Inserted at:** top of §5, trước module tree — rationale per phiếu Decision #2: flow-first orients reader.

---

## Task 3 — Stale "(planned)" sweep

`grep -n "planned\|TBD\|P0[0-9][0-9] (planned)" docs/ARCHITECTURE.md` → **0 hits.**

ARCHITECTURE §1-§10 đã sạch pre-P012 (Architect Anchor #6/#7 đúng). Không có stale reference cần fix.

---

## Cargo.toml additions

- **Added:** `rust-version = "1.85"` (MSRV declaration per PROJECT.md §Tech Stack)
- **NOT added:** `documentation` field (crates.io defaults to docs.rs automatically after publish — no user impact), `exclude` field (.crate 1.3 MiB well under 10 MB threshold)
- **Rationale:** Phiếu Decision #5 khuyến nghị thêm `rust-version`; skip `documentation`/`exclude`.

---

## `cargo publish --dry-run --allow-dirty` output

```
Updating crates.io index
Packaging advisory-inbox v0.1.0 (/Users/nguyenhuuanh/advisory-inbox)
Updating crates.io index
Packaged 116 files, 1.3MiB (414.8KiB compressed)
Verifying advisory-inbox v0.1.0 (...)
Compiling rmcp v1.7.0
Compiling advisory-inbox v0.1.0 (...)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 9.32s
Uploading advisory-inbox v0.1.0 (...)
warning: aborting upload due to dry run
exit=0
```

Không có warning về metadata. Không có `path = "..."` dep. LICENSE + README bundled. Tarball 414.8 KiB compressed — well under limit.

---

## Sub-mechanism Verification Trace

| Sub-mech | Check | Result |
|----------|-------|--------|
| A | N/A (no new trigger) | N/A |
| B | `cargo build --release` | ✅ exit 0, zero warnings |
| B | `cargo test --all` | ✅ 69 tests pass |
| B | `cargo clippy --all-targets -- -D warnings` | ✅ clean |
| B | `cargo fmt --check` | ✅ no diff |
| B | `cargo publish --dry-run --allow-dirty` | ✅ exit 0 |
| C | N/A (no state schema change) | N/A |
| D | `grep -l "INV-LOCAL-002"` | ✅ ARCHITECTURE.md + INVARIANTS.md (2 hits) |
| D | `wc -l README.md` | ✅ 158 ≤ 200 |
| E | `cargo update --dry-run` | ✅ No production dep change; wasip2/wit-bindgen downgrade expected (Rust 1.87 transitive — correct behavior with rust-version=1.85) |
| E | `cargo build --release` clean | ✅ exit 0 |
| F | Token leak grep | ✅ 0 hits on README + ARCHITECTURE + CHANGELOG + Cargo.toml |

---

## Anchors [needs Worker verify] / [unverified] resolution

- **Anchor #5** (CHANGELOG shape): ✅ Verified — shape uses `## P<NNN> — <title>` header + **Type/Tầng/Lane** line + section blocks. P012 entry matches.
- **Anchor #9** (`--dry-run` flag exists): ✅ Cargo 1.94.1 supports `--dry-run` + `--allow-dirty`.
- **Anchor #11** (.gitignore không ẩn LICENSE/README): ✅ grep xác nhận 0 hits.
- **Anchor #12** (.crate size): ✅ 1.3 MiB total — `exclude` not needed.
- **Anchor #13** (CHANGELOG.md exists): ✅ confirmed.
- **Anchor #14** (Sub-mech F token leak): ✅ 0 hits.
- **Anchor #15** (`cargo publish --dry-run` exit 0): ✅ confirmed.
- **Anchor #17** (Discovery Report): ✅ this document.
- **Anchor #18** (`--dry-run` không cần credentials): ✅ confirmed — ran without `~/.cargo/credentials.toml`.

---

## Scope creep moments avoided

- Không thêm `documentation = "..."` vào Cargo.toml (không cần, crates.io tự link docs.rs).
- Không thêm `exclude = [...]` vào Cargo.toml (tarball size acceptable).
- Không sửa bất kỳ file nào trong `src/` (zero code change enforced).
- Không sửa `tests/` (69 tests preserved unchanged).

---

## Kiến trúc sư đọc phần này cho P013

- README mới (158 lines) có `.mcp.json` example đủ để P013 copy vào tarot config. Example path `/path/to/advisory-inbox` — P013 cần update thành absolute path sau `cargo install`.
- Cargo.toml có `rust-version = "1.85"` — P013 `cargo install` sẽ reject Rust < 1.85 (expected behavior; tarot environment dùng rustup nên OK).
- `cargo publish --dry-run` exit 0 → metadata sẵn sàng cho real publish sau P013 confirm end-to-end.

## Discovery Report — P013

**Phiếu:** P013-install-in-tarot
**Date:** 2026-05-28
**Tầng:** 1 (security slash command touch)
**Lane:** Guarded

---

### Assumptions trong phiếu — ĐÚNG:

- Anchor #1 ✅ — `scan-and-append` subcommand exists with `--inbox`, `--state` flags; stdin is automatic when `--report` is OMITTED.
- Anchor #2 ✅ — `migrate-state --state` and `state-backfill --state --inbox` confirmed shipped (P007/P008).
- Anchor #3 ✅ — `advisory-inbox --version` = `advisory-inbox 0.1.0`. Cargo.toml version confirmed.
- Anchor #4 ✅ — All three tarot paths confirmed: `.claude/commands/advisory-scan.md` (142 lines), `docs/security/.advisory-scan-state`, `docs/security/advisory-inbox.md`.
- Anchor #5 ✅ — Tarot CHANGELOG at `~/tarot/docs/CHANGELOG.md` (not root). Newest-first format confirmed.
- Anchor #6 ✅ V2 — State file format: JSON-without-schema_version (Path C). Content exactly as V2 predicted: `{"last_scan_at":"2026-05-26T01:50:00Z","seen_advisories":["CVE-2026-9256","CVE-2026-27205"],"agent_version":"advisory-watch@0.1.0"}`. `jq '.schema_version'` = null. Path C jq pre-step applied.
- Anchor #7 ✅ — `grep` in `~/tarot/CLAUDE.md` returns 2 hits referencing `/advisory-scan` by name only — no "142-line" or "heredoc" wording. No update needed.
- Anchor #8 ✅ — `cargo clean && cargo install --path . --locked` exit 0. Build time ~51s from clean target.
- Anchor #9 ✅ — `~/.cargo/bin/` is on PATH (standard `rustup` setup). `which advisory-inbox` = `/Users/nguyenhuuanh/.cargo/bin/advisory-inbox`. Slash command uses bare `advisory-inbox` invocation.
- Anchor #11 ✅ — Front-matter `description:` field present in rewritten slash command.
- Anchor #13 ✅ — Sub-mech C baseline = 2. Post-Path C and post-smoke-test (cleaned): count = 2. No regression.
- Anchor #14 ✅ — `cargo install --path ~/advisory-inbox --locked` exit 0; `~/.cargo/bin/advisory-inbox` present.
- Anchor #15 ✅ — `cargo clean && cargo install --path . --locked` exit 0 (fresh build, ~51s).
- Anchor #16 ✅ — 0 token patterns in slash command + CHANGELOG.
- Anchor #17 ✅ — Tarot branch `main` confirmed.
- Anchor #18 ✅ — No `~/tarot/.github/pull_request_template.md`. PR body authored from scratch.
- Anchor #19 ✅ — Path C jq pre-step yielded valid JSON v1 schema (all fields present + `schema_version: 1` added).
- Anchor #20 ✅ — `--report` omit form confirmed. `--report -` exits 2 with "No such file or directory". All instruction sections in phiếu V2 use omit form.

---

### Assumptions trong phiếu — SAI so với code thật:

- **V1→V2 already logged in Debate Log Turn 1:**
  - O1.1 [shape]: Phiếu V1 assumed 2 state format paths (A/B). Reality: 3rd format (JSON-without-schema_version). V2 added Path C. Resolved.
  - O1.2 [mechanical]: Phiếu V1 used `--report -` for stdin. Reality: binary treats `-` as literal filename → exit 2. Correct form is omit `--report`. V2 corrected. Resolved.

- **Schema mismatch discovery (EXECUTE, not pre-CHALLENGE):** During smoke test, Worker tested 10-column rows (matching tarot's existing P282 inbox schema) and got `error: expected 8 cells, got 10` from `parse-report`. Investigation revealed:
  - `@agent-advisory-watch` handbook (Bước 4 + Bước 5) specifies **8-column rows** in sentinel block output.
  - Tarot's **existing inbox rows** are 10-column — written by the old P282 Bash heredoc with an extended schema.
  - The binary correctly handles 8-column rows from the agent. The 10-column inbox rows are historical artifacts from the Bash era.
  - **Not a blocker** for P013: future agent runs will emit 8-column rows (per current handbook); binary appends them correctly; tarot will have a mixed-schema inbox (10-col historical + 8-col new). This is acceptable per Sếp's solo+markdown-gate workflow. Logged here for P014 backlog consideration.

---

### Edge cases / limitations phát hiện thêm:

- **Anchor #9 — PATH**: `~/.cargo/bin/` IS on PATH (standard rustup). Bare `advisory-inbox` invocation used in slash command body.
- **Smoke test approach**: Used Approach B (direct binary pipe with 8-column stub report). Approach A (full `/advisory-scan` invocation) was not feasible from subagent context. Logged explicitly per Decision #4 fallback protocol. Sub-mech A proxy via B is sufficient to verify binary executes correctly; full slash command invocation requires a real tarot Claude Code session.
- **State file `last_scan_at` update semantics**: During smoke test execution (Approach B), `scan-and-append` updated `last_scan_at` to the current timestamp. This is expected behavior — the binary always updates state on successful run. Smoke test artifacts were cleaned by restoring the backup snapshot.
- **Inbox mixed schema**: As noted above, tarot inbox now has both 10-column rows (historical, from Bash era) and will receive 8-column rows going forward (from binary via advisory-watch agent). No functional breakage — binary's `state-backfill` extracts advisory IDs (column 2) regardless of total column count; `append` inserts rows verbatim.
- **`cargo update --dry-run` note**: Shows `wasip2` and `wit-bindgen` would downgrade from v1.0.3 and v0.57.1 to v1.0.1 and v0.46.0 — these crates require Rust 1.87 while our MSRV is 1.85. This is expected and correct behavior (lockfile pins to MSRV-compatible versions). Not a surprise major bump.

---

### Docs đã cập nhật theo discoveries:

- **Suggested P014 — `migrate-state` Path C auto-detect**: Future P014 could enhance `migrate-state` to auto-detect "JSON-without-schema_version" and add `schema_version: 1` idempotently. For P013 the jq pre-step was used — fully reversible and doesn't require shipping new `src/` code. Out-of-scope for P013 per Turn 1 RESPOND.
- **Suggested P015 — inbox schema alignment**: Consider aligning `@agent-advisory-watch` Bước 4 row format with tarot's current 10-column P282 schema, OR aligning tarot's inbox back to 8-column for a consistent schema throughout the pipeline. Currently the binary enforces 8-column parse but advisory-watch outputs 8-column — so they match. The tarot inbox's existing 10-column rows are legacy artifacts. Worker notes this for Architect review.
- **Tarot CLAUDE.md/README**: Anchor #7 confirmed no update needed (no "142-line" or "heredoc" references; `/advisory-scan` references are slot name descriptions only).

---

### Layer 2 capability checks fired (Sub-mech A-F):

| Sub-mech | Check | Result |
|----------|-------|--------|
| **A (trigger)** | Approach B: `cat stub-report.md \| advisory-inbox scan-and-append --inbox ... --state ...` | ✅ Exit 0, `{"appended":1,"skipped_dedup":0,"total_open":1}`. Approach A (full /advisory-scan) not feasible from subagent — logged per Decision #4 fallback. |
| **B (capability)** | `cargo clean && cargo install --path . --locked` | ✅ Exit 0. Binary installed at `~/.cargo/bin/advisory-inbox` (~2.16 MB). Build time ~51s from clean. |
| **B (capability)** | `advisory-inbox --version` | ✅ `advisory-inbox 0.1.0` |
| **B (capability)** | `advisory-inbox --help \| grep scan-and-append` | ✅ `scan-and-append  Composite: parse → dedup → append + state update` |
| **B (capability)** | `echo "" \| advisory-inbox scan-and-append --inbox ... --state ...` (empty stdin, `--report` omitted) | ✅ Exit 1, `error: missing sentinel start marker` — correct error for empty/missing sentinel block |
| **C (Path C migration)** | `jq '. + {"schema_version": 1}' .advisory-scan-state > /tmp/state-fixed.json && mv` | ✅ Exit 0; post: `jq '.schema_version'` = 1; `jq '.seen_advisories \| length'` = 2; all original fields preserved |
| **C (preservation)** | Post-install `seen_advisories \| length` vs pre-install baseline | ✅ 2 ≥ 2. CVE-2026-9256 and CVE-2026-27205 both preserved. |
| **D (persistence)** | `grep -l "advisory-inbox" tarot/docs/CHANGELOG.md tarot/.claude/commands/advisory-scan.md` | ✅ Both files reference advisory-inbox |
| **D (persistence)** | `ls advisory-inbox/docs/discoveries/P013.md && grep "P013" advisory-inbox/docs/DISCOVERIES.md` | ✅ Both present (this file + index entry) |
| **E (env drift)** | `cargo clean && cargo install --path . --locked` from clean `target/` | ✅ Exit 0 |
| **E (env drift)** | `cargo update --dry-run` | ✅ No surprise major bump (wasip2/wit-bindgen MSRV-constrained downgrade is expected and correct) |
| **F (runtime state)** | `grep -E 'ghp_\|gho_\|ghu_\|ghs_\|github_pat_' advisory-scan.md CHANGELOG.md` | ✅ 0 hits |
| **F (runtime state)** | `grep -E 'ghp_\|...' tarot/.git/config` | ✅ 0 hits |

---

### Lane assignment + override (if any):

- Classifier output: **Guarded** (security slash command touch — per RULES.md §1 "Touch security boundary → AUTO Tầng 1")
- Reason files: `~/tarot/.claude/commands/advisory-scan.md` (security boundary)
- Override: none
- Security review: `/security-review` on tarot PR #579 — **MUST be invoked by orchestrator post-push** (RULES.md §16 Guarded lane step 7). Worker cannot invoke from subagent context.

---

### Cross-repo notes:

- **Tarot branch:** `feat/advisory-inbox-binary-install`
- **Tarot PR:** https://github.com/aspelldenny/tarot/pull/579
- **Tarot HEAD before P013:** `d168255d7ae0eb5c7c311eab1f1a9a760c7c254c`
- **advisory-inbox HEAD:** `f569f6d` (main at time of P013 execution)
- **Heredoc line count:** 142 → 27 lines
- **State file format path taken:** Path C (JSON-without-schema_version → JSON v1 via jq pre-step)
- **Smoke test approach:** B (direct binary pipe; full Approach A requires real tarot Claude Code session)

---

### Debug Log:

```
[2026-05-28] event=snapshot evidence=advisory-inbox/.backup/P013/ created; seen-count-before=2; tarot-head=d168255d7ae0
[2026-05-28] event=cargo_install evidence=exit=0 binary=/Users/nguyenhuuanh/.cargo/bin/advisory-inbox version=0.1.0 build_time=~51s (clean target)
[2026-05-28] event=tarot_state_format evidence=first-char='{' format=json-without-schema_version seen_count=2 (CVE-2026-9256, CVE-2026-27205)
[2026-05-28] event=path_c_jq_prestep evidence=exit=0; post: schema_version=1 seen_count=2 all_fields_preserved=true
[2026-05-28] event=slash_rewrite evidence=~/tarot/.claude/commands/advisory-scan.md lines_before=142 lines_after=27 wc_check=pass
[2026-05-28] event=smoke_test_approach_b evidence=exit=0 output={"appended":1,"skipped_dedup":0,"total_open":1} dedup_rerun={"appended":0,"skipped_dedup":1,"total_open":1}
[2026-05-28] event=schema_mismatch_finding evidence=10-col tarot inbox rows cause parse-report exit=2 "expected 8 cells, got 10"; advisory-watch agent emits 8-col rows per handbook Bước 4; not a blocker
[2026-05-28] event=smoke_cleanup evidence=inbox+state restored to Path C canonical state from backup
[2026-05-28] event=tarot_commit evidence=b22ebdd; 3 files changed, 29 insertions(+), 133 deletions(-)
[2026-05-28] event=tarot_pr evidence=https://github.com/aspelldenny/tarot/pull/579
[2026-05-28] event=sub_mech_F evidence=0 token patterns in slash+CHANGELOG+git/config
```

---

### Pilot retrospective notes (Workflow v2.1):

- Debate Log Turn 1 caught 2 objections pre-EXECUTE (O1.1 [shape]: 3rd state format unknown to phiếu V1; O1.2 [mechanical]: `--report -` doesn't accept stdin). V2 RESPOND addressed both surgically. Turn 2 CHALLENGE confirmed V2 resolves both. This is exactly the Workflow v2.1 Guarded-lane debate cycle working as intended.
- **Note for sos-kit golden template**: future architect prompts should include "verify CLI stdin idiom (`-` vs omitted flag) via `--help` BEFORE writing scan-and-append invocation samples". O1.2 was a [mechanical] correction that could have been caught at Bước 0 with a 30-second `--help` check.
- **Note for sos-kit golden template**: cross-repo phiếu anchors on remote repo file content are necessarily `[needs Worker verify]` — this is correct and expected. CHALLENGE mode verifying those anchors (paths, format) before EXECUTE is valuable. P013 is the first cross-repo phiếu in this pilot.
- **Smoke test approach gap**: Approach A (full slash command invocation) requires a real Claude Code session with `/advisory-scan` available — not feasible from subagent context. Future workflow doc should clarify: for cross-repo slash command integration tests, Approach B (direct binary pipe) is the standard fallback for subagent EXECUTE. Sub-mech A "trigger verified" should document which approach was used.
